usersByQuery = select login, password, true from users where login=?
rolesByQuery = select login, role.role_name from users, (select id, role_name from user_roles) as role where (users.role_id = role.id) and (login = ?)

